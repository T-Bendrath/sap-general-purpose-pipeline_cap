_schema-version: "3.1"
ID: sap.capire-bookshop
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
    - cds build/all
description: A simple bookshop application, build in a self-contained all-in-one fashion, i.e. w/o reusing other packages.
modules:
- name: capire-bookshop-srv
  path: gen/srv
  properties:
    EXIT: 1
  provides:
  - name: srv-binding
    properties:
      srv-url: ${default-url}
  requires:
  - name: capire-bookshop-db
  type: nodejs
- name: db
  parameters:
    app-name: capire-bookshop-db
  path: gen/db
  requires:
  - name: capire-bookshop-db
  type: hdb
parameters:
  enable-parallel-deployments: true
resources:
- name: capire-bookshop-db
  parameters: null
  properties:
    hdi-service-name: ${service-name}
  type: com.sap.xs.hdi-container
version: 1.0.0-20210211123038+3d447358f264f9441bb4d6e453ff2133a5801c3d
