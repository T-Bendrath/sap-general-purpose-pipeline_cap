### General project setup
general:
  productiveBranch: 'main'
  buildTool: 'mta'
  gitSshKeyCredentialsId: githubCredentialsId
  verbose: true

### Step-specific configuration
steps:
  handlePipelineStepErrors:
    failOnError: true

  artifactSetVersion:
    gitPushMode: 'NONE'
    commitVersion: false

  uiVeri5ExecuteTests:
    # installCommand: "npm install @ui5/uiveri5 --global --quiet && npm install @sap/cds --global --quiet && npm install @sap/cds-dk --global --quiet && cds watch"
    runOptions: ["./uiveri5/conf.js"]

  # cloudFoundryDeploy:
  #   cloudFoundry:
  #     credentialsId: 'cfCredentialsId'
  #     org: 'cf-dts-integration-de'
  #     space: 'openSAP'

### Stage-specific configuration
stages:
  # 'Pull-Request Voting':
  #   useCloudMbt: true
  #   failOnError: true

  'Central Build':
    buildTool: 'mta'

  'Acceptance':
    # active: true
    cloudFoundryDeploy: false
    uiVeri5ExecuteTests: true

  'Confirm':
    manualConfirmation: false

  # 'Release':
  #   cloudFoundryDeploy: true
